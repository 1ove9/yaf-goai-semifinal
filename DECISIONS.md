# YAF Architecture Decision Records (ADR)

## ADR-001: Pydantic v2 domain models over dataclasses
**Decision**: All domain entities use Pydantic v2 `BaseModel`.
**Rationale**: JSON Schema auto-generation for FastAPI, strict validation at creation, serialization to JSONB for PostgreSQL. Pydantic v2 is 5-30x faster than v1.

## ADR-002: Protocol over ABC for adapters
**Decision**: SolverAdapter, AIBackend, CADBackend use `typing.Protocol` with `@runtime_checkable`.
**Rationale**: Plugins can satisfy the protocol without inheriting from a base class, enabling hot-loading via `plugin.toml`.

## ADR-003: JAX over PyTorch for differentiable FDTD
**Decision**: `yaf_ai/differentiable/` uses JAX + Flax + Optax.
**Rationale**: JAX provides native autodiff through control flow (jax.lax.scan for time-stepping), XLA compilation, and seamless GPU/TPU scaling.

## ADR-004: scikit-rf as mandatory RF dependency
**Decision**: S-parameter handling, Touchstone I/O, and network analysis use scikit-rf when available.
**Rationale**: scikit-rf is the de facto Python standard for RF/microwave engineering.

## ADR-005: Structured logging with structlog over standard logging
**Decision**: All modules use structlog with design_id/job_id/solver bound variables.

## ADR-006: In-memory stores for API demo, PostgreSQL for production
**Decision**: API routers use dict-based stores by default, SQLAlchemy models ready for production.

## ADR-007: Analytical fallback for solver adapters
**Decision**: When solver executables are unavailable, adapters compute results analytically (induced EMF for dipoles, array factor for metasurfaces).

## ADR-008: uv for Python, pnpm for frontend
**Decision**: Package management split: uv for Python dependencies, pnpm for Node.js.

## ADR-009: No separate RPC framework at v0.1
**Decision**: REST for CRUD, WebSocket for streaming. No gRPC yet.

## ADR-010: SIMP density method for topology optimization
**Decision**: Density-based SIMP with optimality criteria update.

## ADR-011: VAE latent space for design generation default
**Decision**: Pipeline default uses VAE for candidate generation. Diffusion is optional upgrade.

## Decision Log

| Timestamp | Decision | Reason |
|-----------|----------|--------|
| 2026-05-15 | OpenEMSAdapter analytical fallback | Enables testing without solver binary |
| 2026-05-15 | NEC2Adapter induced EMF method | Physics-realistic S11/gain for demo |
| 2026-05-15 | VAE 32x32 grid input | Resolution vs training speed balance |
| 2026-05-15 | PML thickness=8 cells | Standard FDTD literature value |
| 2026-05-15 | Cholesky GP implementation | Stable for <1000 observations |
| 2026-05-15 | Vite proxy /api → localhost:8000 | Zero CORS config in dev |
| 2026-05-20 | docker-compose: drop obsolete `version: "3.9"` key | Modern Docker Compose emits a deprecation warning that breaks CI parsers; the key is now ignored anyway. |
| 2026-05-20 | Skim/skimage marching_cubes over `trimesh.creation.marching_cubes` | `trimesh.creation.marching_cubes` was removed upstream years ago; `skimage.measure.marching_cubes` is the canonical replacement and is already an indirect dep through trimesh. |
| 2026-05-20 | diff_fdtd_jax source uses physical-Hz timing and `compute_s11` plumbs eps through the time-loop | The normalized source frequency (`f * dx / c0 = 1.0`) aliased to phase 0 on every time step, producing zero gradient; the loss function was also reading `self.eps_r` rather than the passed parameter, so gradients couldn't flow at all. Acceptance §6 requires loss decrease. |
| 2026-05-20 | VAE demo persists weights to `models/vae_designer.pt` | Acceptance §6 requires "training completes, weights saved" verbatim; previous `train_demo` returned without calling `designer.save()`. |
| 2026-05-20 | Material library seeds 19 entries (added `air` + `teflon`) | Unit test `test_materials_seeded` requires ≥18 materials. Vacuum was already there but isn't useful at runtime — air is the everyday dielectric value, teflon covers the most common low-loss substrate. |
| 2026-05-20 | mypy: keep `strict = true`; per-module `ignore_missing_imports` for vendor libs without stubs (OCC, openems, CSXCAD, …) | Acceptance command is `mypy ... --strict` and now succeeds (0 issues, 64 files). Stubless 3rd-party modules are silenced at the import level (per-module override block), not via per-line `# type: ignore`. |
| 2026-05-20 | Use `np.trapezoid` instead of `np.trapz` (oam.py) | numpy 2.x removed `np.trapz` (deprecated in 1.26). The codebase already pinned numpy ≥1.26 in pyproject. |
| 2026-05-20 | YAF installed editable with `pip install --no-deps -e .` | The full editable install pulled orbax-checkpoint, whose test-fixture path exceeds Windows MAX_PATH and breaks pip on this host. `--no-deps` is safe because all transitive deps were already installed via `pip install` of the dep groups. |
| 2026-08-09 | Freeze Day 3 cross-solver protocol `day3-air-wire-grid-v1` before running evidence | NEC2 cannot model FR4 directly. Cross-check an invariant-metal air variant using real openEMS and an explicit finite equal-area NEC2 wire grid; require both <=5% resonance deviation and <=3.0 dB S11-depth deviation. Divergence remains evidence. |
| 2026-08-09 | Use constrained random growth and seeded (1+1) evolution for 16x16 pixel topology | High-dimensional binary masks are not a valid use of the existing continuous GP. Both pixel proposers share one frozen space, fixed feed, symmetry/connectivity rules, and exact RLE/hash reconstruction. |
| 2026-08-09 | Freeze cross-solver protocol `day4-native-curves-v2` before its first numeric run | The v1 wifi24 case showed a 32.57 dB S11-depth delta, demonstrating that matching depth is too feed-sensitive for a binary test. V2 requires resonance agreement plus common-band curve correlation, and gates all work on a stricter native half-wave-dipole anchor. |
| 2026-08-09 | Attribute the Day 3 patch discrepancy with fixed 6/12/24 NEC2 grids | A monotonic gap reduction below half the grid-6 gap means an instrument boundary; a grid-24 gap at least 80% of grid-6 means a genuine anomaly; intermediate trends require finer-grid extrapolation. |
| 2026-08-09 | Use NEC2 as the Day 4 wire exploration oracle and openEMS as verifier | A meander centerline is native to both MoM wire and FDTD wire/thin-box models, eliminating the lossy patch-sheet-to-wire-grid transformation while retaining independent numerical methods. |
| 2026-08-09 | Freeze cross-solver protocol `day4-wideband-resonance-v2.1` before Day 4.5 numeric work | Day 4 v2 incorrectly confirmed two flat, badly mismatched curves whose minima were both at sample index zero. V2.1 requires an interior minimum deeper than or equal to -6 dB before agreement metrics, uses a 1.5--3.5 GHz 201-point cross-check sweep, and otherwise inherits v2 unchanged. |
| 2026-08-09 | Freeze Day 4.5 wire score `wire-realized-gain-v2` before batch v5 | NEC2 reports gain relative to accepted power, so the wire score applies `gain_dbi + 10*log10(1-|Gamma|^2)` using the best sampled S11, retains a half-weight best-VSWR term, and assigns zero weight to lossless-NEC2 efficiency. Both terms are normalized power ratios in [0,1]. Native centerlines use adaptive odd segmentation no longer than lambda/20 at the sweep maximum. |
| 2026-08-09 | Freeze `meander-dipole-v2-5d` and the Day 5 v6 matrix before numeric work | The actual v1 corner audit was 28.7--246.0 mm with 10/80 invalid corners, not a turns<=2.6 hard limit. V2 directly parameterizes 50--80 mm total centerline length around the 61.182134 mm half-wave estimate, retains turns 2--6, and has 80/80 valid boundary cases with pitch>=1.5 mm. GP and Random share it; `wire-realized-gain-v2` is unchanged. The n=400 GP suggestion benchmark was 0.033720 s, so all observations are retained. |
| 2026-08-09 | Freeze Day 5 top-2 selection and meander convergence plan before batch v6 | Rank every accepted GP evaluation by target-band valid resonance first (depth<=-6 dB and minimum outside three edge samples), then score descending, run ID ascending, and step index ascending; deduplicate geometry hashes and select exactly two before cross-check. Top-1 uses NEC2 lambda/20, /40, /80 against 2x openEMS; inherited ratio thresholds classify attribution, default-vs-2x openEMS <=3% is the self-convergence check, and final v2.1 checks use lambda/80 plus 2x without changing any protocol threshold. |
| 2026-08-09 | Abort `day5-wire-v6r1` and freeze `meander-dipole-v2.1-5d` before retry | Four GP seed winners had no valid target-band internal resonance and three hit the 80 mm upper boundary. The fixed top-1 diagnostic measured 2.560 GHz in lambda/20 NEC2 and 2.720 GHz in 2x openEMS, implying 83.484/88.703 mm at 2.45 GHz. Retry `day5-wire-v6r2` changes only the direct length upper bound to 100 mm; score, protocol, matrix, top-2 rule, and convergence plan remain frozen. |
| 2026-08-09 | Freeze Day 5-1b converged-instrument execution without changing protocol v2.1 | Candidate A is v6r2 GP seed 202 step 255 (score-ranked top-1); candidate B is the deepest target-band S11 across all 4001 archived rows, seed 202 step 253, with deterministic score/run/step tie-breaks. openEMS runs 4x then only if preregistered timing/convergence rules require it 8x or 3x; NEC2 appends lambda/160. Internal/depth/5%/Pearson>=0.8 gates remain byte-for-byte unchanged. Both candidates are always reported, and either one CONFIRMED pre-authorizes the environment-level confirmed finding. |
| 2026-08-09 | Freeze the final wifi24 patch air-variant execution before any Day 5-2 run | The unique candidate is `day2-wifi24-gp-s505` top-0/step-16 with the unchanged Day 3 air and equal-area wire-grid mappings. Reuse the exact 4.098041023--6.830068372 GHz/51-point sweep and unchanged v2.1 gates. openEMS reuses 1x then runs 2x (4x only if >3% self-shift); NEC2 runs grid 32/36/44 only while cubic time is <=3 hours and one complex128 dense matrix is <=80% of currently available physical memory. All completed points refit the frozen log-log gap roadmap; only new Day 5-2 documents may resolve the Day 2 verdict. |
| 2026-08-11 | Freeze mesh-count interpretation before auditing the Day 5-2 patch 2x claim | Build, but do not solve, the unchanged `day2-wifi24-gp-s505` air variant at openEMS refinement 1.0 and 2.0; record per-axis grid lines, total cells, and cell-size extrema. A 2x/1x cell ratio >=3 establishes effective refinement, 1.2--3 is partially supported, and <1.2 is ineffective and retracts the self-convergence claim. Only the latter two outcomes authorize a minimal non-1x propagation fix and one real 2x rerun under the unchanged 3% movement gate. The final Day 5-2 DIVERGENT verdict is not reopened. |
| 2026-08-11 | Freeze the archive-only Chu--Harrington benchmark before computing any Q value | Use the McLean `1/(ka)^3 + 1/(ka)` lower bound, actual reconstructed enclosing spheres, and a magnitude-only single-resonance RLC reflected-power proxy with a half-notch-power window. R-squared below 0.9, fewer than five fit samples, invalid resonance gates, missing crossings, or a physically inconsistent Q/Qmin below one exclude a row from the main table and plot. Fractional-bandwidth Q uses the same crossings; disagreements above 30% are reported, never hidden. Candidate A/B, the native dipole anchor, and the final repaired-openEMS/grid-44 patch pair are the frozen primary sources. No solver call or new run is permitted. |
| 2026-08-11 | Freeze Day 6 free-form 3D dual-band exploration before numerical work | Use a centrally symmetric single-feed 3D centerline in a 40 mm cube, select the largest instrument-valid N in {5,6,7} by the committed deterministic preflight, and give GP and Random the same 3N-dimensional bounds. The primary classic is a preregistered 20x20 body-diagonal OCFD scan, not a straw-man single-frequency dipole. Each band's score is accepted-power fraction and the total is their minimum. Exactly two score-ranked, geometry-deduplicated GP candidates are frozen before cross-check. Positive discovery additionally requires >=10% over the optimized OCFD, valid <=-6 dB resonances and <=5% frequency gaps in both bands, whole-sweep Pearson >=0.8, lambda/160 NEC2, and a separately self-converged 6.5-GHz-governed openEMS mesh. Native CSXCAD Curve serialization is mandatory for non-axis-aligned wires; diagonal bounding boxes are forbidden. |
| 2026-08-11 | Separate the Day 6 real exploration oracle from the final lambda/160 verifier before references or batch | Nine archived lambda/160 probes all returned 251 real subprocess samples but took 17.390--226.109 seconds, so the frozen 10-second rule selected no N and correctly stopped before OCFD. The initial text had over-applied the final v2.1 instrument to every exploration evaluation. New r3 probes, the fair OCFD scan, and GP/Random use real NEC2 lambda/20 under the unchanged 10-second and three-hour gates; exactly the two preregistered winners are later rerun at lambda/160. No geometry, score, reference grid, top rule, or discovery threshold changes. |
| 2026-08-11 | Freeze Day 6 N=7, budget 200, full-observation GP, and the optimized OCFD before batch | All r3 probes passed the unchanged 10-second gate, so the largest N=7 (21 dimensions) is selected. A 300-observation GP suggestion took 0.018513 s, so no sliding window is used. The slowest N=7 probe plus GP cost selects budget 200 at a conservative 9225.025 s for ten cells. The 20x20 OCFD winner is 53.842105 mm at offset ratio 0.221052632, score 0.617137421; it beats the 0.100343450 straight control and passes the frozen scoring sanity check. Config hash is `f01bcf4de5b136fe096b10b9f658e28e4ab402f473c80d3eca5125ee7e4a0a89`. |
| 2026-08-11 | Retain the Day 6 lambda/160 instrument and raise only its process timeout before retry | Candidate A's first final NEC2 invocation used the frozen lambda/160 geometry and 251-point sweep but hit the adapter's generic 300-second subprocess timeout before producing a curve; `YAF_NO_FALLBACK=1` correctly stopped the workflow. The failed attempt is archived as `day6-freeform-final-crosscheck-top1-timeout300`. Retry uses the same two committed candidates, geometry, segmentation, sweep, score, openEMS 2x instrument, and every v2.1 threshold; only `nec2_timeout_seconds=1800` changes. This is an execution-capacity correction made without seeing a lambda/160 numeric result, not a result-driven discretization or candidate retry. |
| 2026-08-12 | Freeze Day 6.5 free-form renderer repair, rotation release gate, re-verdict, and ES hunt before numerical work | Replace only the failed free-form CSXCAD `Wire` representation with balanced lambda(6.5 GHz)/40 axis-aligned PEC-box stairs and the meander path's mesh-defined cross-section rule. Release requires the same half-wave dipole in axis, xy-45, and xz-45 orientations to pass valid -6 dB resonances plus every pair's <=2% frequency shift, <=1.5 dB depth change, and Pearson >=0.95. Day 6 A/B remain frozen and cannot become a dual-band discovery because A's NEC2 high band is already -5.147263 dB. The new N=7 batch is restarted (1+1)-ES versus Random, three seeds, budget 500; ES alone observes a fixed +0.25 both-bands-valid bonus, while reporting, top-2 ranking, OCFD comparison, and unchanged v2.1 verdicts use the unshaped worst-band accepted-power FoM. |
| 2026-08-12 | Keep the failed Manhattan gate and freeze mesh-supported true-wire renderer iteration 2 before retry | The first rotation run failed honestly: openEMS axis versus either 45-degree orientation differed by 7.962529%, about 4.70 dB, and Pearson about 0.485, while NEC2 was rotationally invariant. This rejects the longer taxicab current path. R2 retains the exact finite-radius Euclidean `Wire`, inserts collinear support vertices at no more than lambda(6.5 GHz)/40, and brackets every support coordinate on all Yee axes. Run ID changes to `day65-freeform-rotation-invariance-r2`; geometries, sweep, solver settings, and all release thresholds remain unchanged, and later work stays blocked until r2 passes. |
| 2026-08-12 | Keep the failed supported-Wire gate and freeze a solid supercover tube for renderer iteration 3 | R2 preserved the axis resonance but both oblique `Wire` curves again stayed near 0 dB, proving that dense collinear support does not rescue the installed 1D primitive. R3 lays overlapping fixed-0.5-mm axis-aligned PEC cubes at <=0.25 mm spacing along the true Euclidean centerline and puts every cube face on the Yee grid. The surrogate width is disclosed and remains subject to the unchanged cross-solver gap; run ID changes to `day65-freeform-rotation-invariance-r3`, while all rotation geometries and <=2% / <=1.5 dB / Pearson>=0.95 release gates remain frozen. |
| 2026-08-12 | Reject r3 before simulation on mesh-size grounds and freeze finite-cylinder renderer iteration 4 | The 472.676 mm candidate would require about 1,891 r3 voxel centers and a multi-billion-cell rectilinear worst case, so r3 cannot support its own required 1x/2x instrument and is not run. R4 maps each true centerline edge to an arbitrary-axis PEC `Cylinder` of fixed 0.25 mm surrogate radius and uses a bounded 0.5 mm/refinement local uniform grid (about 0.5M/4.2M cells for the 40 mm box). Run ID becomes `day65-freeform-rotation-invariance-r4`; physical endpoints, feed gap, source geometries, sweep, and every release threshold remain unchanged. |
| 2026-08-12 | Keep the failed r4 1x result and refine only the rotation instrument to 2x for r5 | R4 proved both oblique cylinders render consistently (2.110 GHz, pair Pearson 0.999995), but the 1x axis case was 1.870 GHz, a 12.060302% direction error. R5 keeps the exact cylinders, fixed 0.25 mm surrogate radius, source geometries, sweep, and release thresholds, and changes only the preregistered openEMS grid from 0.5 mm to 0.25 mm via refinement 2x. Run ID is `day65-freeform-rotation-invariance-r5`; later work remains blocked unless all unchanged gates pass. |
| 2026-08-12 | Correct the rotation fixture's feed-angle confound before r6 without relaxing its gate | R5 still differed by 9.259259%, but its x-directed lumped gap stayed fixed while arms rotated x->xy/xz, changing the feed-arm angle and therefore not rigidly rotating the complete fed structure. R6 keeps the x port byte-identical and rotates equal-length arms about that feed axis through y-axis, yz-45, and z-axis directions, so all cases share the same 90-degree junction while testing two Cartesian axes and one oblique 3D direction. The r4 cylinder, 2x grid, sweep, and <=2% / <=1.5 dB / Pearson>=0.95 gates are unchanged; run ID is `day65-freeform-rotation-invariance-r6`. |
| 2026-08-12 | Keep the near-threshold r6 failure and refine only the corrected rotation instrument to 4x | R6 made y/z axes agree exactly (2.190 GHz, Pearson 0.999983), but yz-45 was 2.130 GHz: 2.777778% and Pearson about 0.899 still fail the frozen thresholds. R7 changes only the local grid from 0.25 mm to 0.125 mm via openEMS refinement 4x. Cylinders, fixed radius, corrected rigid-rotation family, sweep, and <=2% / <=1.5 dB / Pearson>=0.95 gates remain unchanged; run ID is `day65-freeform-rotation-invariance-r7`. |
| 2026-08-12 | Retry the unobserved r7 gate with only a longer openEMS process timeout | The first r7 invocation wrote no run and no numeric comparison because a real 4x subprocess exceeded the generic 900-second per-solve timeout after 1,313 seconds of total execution; strict no-fallback stopped it. R7r2 raises only `openems_timeout_seconds` to 1800 and preserves the underlying subprocess error in strict mode. Geometry, 4x grid, sweep, six curves, and every rotation threshold remain unchanged. |
| 2026-08-12 | Correct the free-form FDTD physical-duration confound before r8 | R7r2 made y/z agree exactly but lost the oblique valid resonance. The renderer had fixed 40,000 time steps while spatial refinement shortened the Courant step, so 4x simulated about one quarter of the 1x physical duration. R8 returns to the valid-resonance 2x spatial grid and uses 80,000 maximum steps to preserve nominal physical duration, with the same `1e-4` decay criterion. Cylinders, corrected rotations, sweep, and all release gates remain unchanged; run ID is `day65-freeform-rotation-invariance-r8`. |
| 2026-08-12 | Freeze the next spatial-and-temporal convergence point at 4x/160k for r9 | R8's 2x/80k result reproduced the 2.777778% and Pearson about 0.90 direction error, ruling out the shortened decay window as its cause. R9 combines the next 4x spatial grid with 160,000 maximum time steps and a 3,600-second process timeout, keeping nominal physical duration constant. Geometry, fixed cylinder radius, sweep, decay criterion, and all rotation gates remain unchanged; later work stays blocked unless r9 passes. |
| 2026-08-12 | Make the unobserved r9 execution crash-resumable without changing its instrument | The first r9 process ended with external-termination status `0x40010004` after writing no run and exposing no numerical result; event logs showed neither an application crash nor resource exhaustion, no solver survived, and about 37.7 GiB remained free. Retry atomically stages each completed orientation in ignored `runs/` state and reuses it only under the exact r9 config hash. The final six-curve run remains all-or-nothing; geometry, 4x/160k instrument, timeout, sweep, solver modes, thresholds, and run ID are unchanged. |
| 2026-08-12 | Freeze r10 as a resolved-radius CSXCAD Wire endpoint-connection test | R9 made the 4x frequency gap pass at 1.801802% but the oblique arm differed by 5.254860 dB and Pearson 0.883634. Official CSXCAD defines Wire as a radius-bearing Curve whose inside test includes a radius sphere at every vertex; r2 instead used a 0.05 mm Wire on about 1.5 mm cells and was unresolved. R10 replaces each r9 flat-ended 0.25 mm cylinder arm with one official 0.25 mm sphere-ended Wire on the identical 0.125 mm grid. The 4x/160k instrument, physical centerline, feed, sweep, timeout, solvers, and <=2% / <=1.5 dB / Pearson>=0.95 gates remain unchanged. |
| 2026-08-12 | Remove direction-dependent Cartesian runt cells before rotation gate r11 | R10 passed the frozen frequency gate but failed depth and Pearson. A byte-level XML audit then found that its nominal 0.125 mm local grid was not the same instrument by orientation: forcing every geometry vertex onto a global lattice produced 0.033933 mm minimum cells for y/z and 0.006151 mm cells for yz-45, with 1,336,392 versus 8,468,064 total cells. R11 partitions each structure-bounding-box interval evenly between only the box and feed breakpoints, never exceeding 0.125 mm and rejecting intervals below half that target; resolved sphere-ended Wires no longer require vertices on mesh planes. Radius, geometry, 4x/160k settings, sweep, solvers, and every release threshold remain unchanged. |
| 2026-08-12 | Continue the corrected rotation instrument to 6x/240k as r12 | R11 eliminated runt cells and made y/z agree, while yz-45 passed depth but remained just outside the frozen frequency and Pearson gates at 2.309469% and about 0.9317--0.9319. R12 changes only spatial resolution from 0.125 to 0.083333 mm and maximum steps from 160k to 240k to preserve nominal physical duration; timeout rises to 7,200 seconds as execution capacity. Sphere-ended radius, equal-interval meshing, geometries, sweep, solvers, and every <=2% / <=1.5 dB / Pearson>=0.95 gate remain unchanged. |
| 2026-08-12 | Use the released 6x free-form instrument for the frozen A/B re-verdict | R12 passed every rotation gate at 6x/240k; coarser instruments remain unreleased for claims. Candidate A first receives the originally preregistered repaired-renderer 1x/2x high-band self-convergence check, extending to 4x/6x only if adjacent shift remains above 3%. Final A/B openEMS curves use the released 6x setting and may run up to 21,600 seconds each; immutable lambda/160 NEC2 curves are reused byte-for-byte from their archived Day 6 cross-checks. New run IDs prevent any old evidence overwrite. |
| 2026-08-12 | Freeze the Day 6.5 v2 final instrument before its batch | Candidate-specific 1x/2x high-band convergence remains a required diagnostic and extends through 4x/6x on failure, but only the r12-released 6x/240k free-form openEMS instrument may support a claim. After all six lambda/20 NEC2 runs are archived, exactly two ES designs are committed by the already-frozen unshaped-score/hash-deduplicated rule before solver work. Final top1/top2 use real lambda/160 NEC2 plus 6x/240k openEMS on the unchanged 1.5--6.5 GHz/251-point sweep; every v2.1 and 10%-over-OCFD gate remains unchanged. |
| 2026-08-12 | Reuse candidate A's byte-identical released 6x convergence curve in its final re-verdict | Candidate A's 6x convergence level and final openEMS instrument have the same committed geometry hash, 1.5--6.5 GHz/251-point sweep, refinement 6x, 40,000 base/240,000 effective maximum steps, timeout, and released r12 renderer. The final evidence records the source run ID and validates every setting before reuse, avoiding an unnecessary duplicate solve without changing a byte, candidate, instrument, or verdict gate. Candidate B still receives its one required new 6x solve. |
| 2026-08-13 | Freeze the Day 6.5 systematic frequency-bias and candidate-B compute-feasibility diagnostics before numerical execution | Diagnostic 1 reuses the byte-identical r12 `y_axis` NEC2 control configuration and changes only `wire_radius_m` from 0.00005 to 0.00025 for one real subprocess sweep over the same 1.5--3.5 GHz/201-point window. Define `explained_fraction = (2.330 GHz - f_res@0.25mm) / (2.330 GHz - 2.210 GHz)`; values >=0.8 attribute the bias to the surrogate-radius systematic effect, 0.3--0.8 are partial attribution, and <0.3 rejects the attribution. Diagnostic 2 build-not-solves byte-identical released 6x XMLs for frozen candidates A and B, invoking no openEMS time stepping, and records per-axis line counts, total cells, cell-size extrema, and a disclosed memory estimate. `cells_B/cells_A >=1.35` classifies the timeout as compute-scale-dominated and `infeasible_at_current_compute`, forbidding another B solve; a ratio <1.35 authorizes one future B rerun with only `openems_timeout_seconds=43200`, outside this task. `runs/day65-pipeline.stdout.log` and `runs/day65-pipeline.stderr.log` are the formal candidate-B timeout evidence. This task changes no protocol, threshold, sweep, candidate, or score and starts no ES/random v2 batch. |
| 2026-08-13 | Freeze the Day 6.5 factorial anchor audit and candidate-B terminal retry before any new solve | Every anchor factor reuses the archived r12 `y_axis` 61.182 mm sphere-ended-wire fixture, 1.5--3.5 GHz/201-point sweep, and all settings except the single named variable. Execute in increasing-cost order: F3 runs NEC2 at the already matched 0.25 mm radius with lambda/80 and lambda/320 segmentation; both must stay within 10 MHz inclusive of the archived lambda/160 value 2.290 GHz to classify segmentation as stable, otherwise stop with `nec2_thick_wire_not_converged`. F4-NEC2 then changes only `feed_gap_m` from 0.0006 to 0.0003. F2 changes only each openEMS free-end centerline by -0.25 mm, giving a 60.682 mm end-to-end centerline while retaining the 0.25 mm sphere-ended radius and 6x/240k instrument. F4-openEMS changes only the same feed gap to 0.0003 at 6x/240k. F1 changes only openEMS spatial refinement to 8x and maximum steps to 320,000, preserving nominal physical duration; its process timeout is 7,200 seconds, as for r12 execution capacity. Define `delta_f_F1 = f_res(8x) - 2.210 GHz`, `delta_f_F2 = f_res(shortened) - 2.210 GHz`, `delta_f_F4_nec2 = f_res(0.3mm gap, NEC2) - 2.290 GHz`, `delta_f_F4_openems = f_res(0.3mm gap, openEMS) - 2.210 GHz`, and `delta_f_F4_differential = abs(delta_f_F4_openems - delta_f_F4_nec2)`. For F1 and F2 use the absolute named shift, and for F4 use the differential; >=20 MHz inclusive is `material`, otherwise `minor`. Residual bias is reported per factor as `abs((2.290 GHz + delta_nec2) - (2.210 GHz + delta_openems))`; no additive multi-factor or protocol conclusion is authorized. Any F1--F4 failure or timeout stops all later execution without a configuration retry. Only after those factors succeed, frozen candidate B receives its single previously authorized released-6x retry with byte-identical geometry, 1.5--6.5 GHz/251-point sweep, 240,000 steps, and every verdict gate unchanged; only `openems_timeout_seconds` becomes 43,200. Success is archived as `day65-repair-openems-convergence-top2-6x` and feeds the unchanged top2 re-verdict `day65-repair-crosscheck-top2`; another `TimeoutExpired` terminates it as `infeasible_at_current_compute` with no third attempt absent a future preregistration. This audit changes no protocol, threshold, score, sweep, candidate, existing run, or ES/random state. |
| 2026-08-13 | Record the externally interrupted candidate-B terminal attempt and authorize one exact replacement after host reboot | The 43,200-second candidate-B run authorized by the preceding entry started at `2026-08-13T13:40:38.350250Z`, passed the frozen XML SHA-256 gate (`c0db05cf170b873201529461cf90c4fd5a2c981cd39be1cdce15e911ee3471ba`), and produced no run directory or numeric result before the host restarted at `2026-08-13T15:07:29.500000Z`. Because openEMS provides no persistent time-step checkpoint, this external interruption cannot be byte-resumed. Before replacement execution, archive a zero-result failure record as `day65-repair-openems-convergence-top2-6x-host-reboot`; then preserve the interrupted attempt state as a snapshot and start exactly one replacement with the same target run ID, geometry, XML hash, 6x grid, 240,000 steps, 1.5--6.5 GHz/251-point sweep, 43,200-second timeout, and unchanged verdict gates. This replacement is the final authorized attempt: success feeds the already-frozen top2 re-verdict, while timeout, another host interruption, or any other solver failure ends candidate B without another retry. No protocol, threshold, candidate, score, sweep, existing run, or ES/random state changes. |
| 2026-08-26 | Freeze the semifinal paired-state meander problem and all release gates before implementation or numeric work | `docs/YAF-semifinal-plan-v3.4-final-freeze.md` is the authoritative specification for `ideal-symmetric-telescopic-PEC-meander-v1`: one quantized shared hardware identity, two state controls, the written 40 mm centerline equations, fixed integer-micrometre manual grid, NEC2-only search score, 21-point linear state trajectory, candidate order, openEMS-only independent cross-check, and G0--G7 release sequence. The vertex walk follows the existing audited meander convention exactly: after the final horizontal segment it does not flip `y` or append another vertical segment. The 20 timing-preflight pairs are selected without solver results by sorting all geometry-valid pairs by `SHA256("yaf-semifinal-v3.4-preflight|" + pair_hash)` ascending and taking the first 20; they are timing evidence only and are excluded from every candidate pool, training observation, descriptive statistic, manual-baseline result, warm-parent pool, and discovery claim. No NEC2 or openEMS numerical run is authorized until G0--G6 pass; ES-warm additionally requires the committed manual baseline and frozen parent required by G7. This freeze changes no prior protocol, threshold, run, or artifact byte. |
| 2026-08-26 | Freeze the semifinal 5.8 GHz meander-renderer anchor r2 before any r2 solve | R1 used the exact free-space half-wave length without finite-wire end-effect correction, and its 1x/2x openEMS sequence was still moving toward NEC2 rather than demonstrating convergence. R2 authorizes exactly one NEC2-only length calibration using the archived 5.480 GHz r1 resonance, then runs the unchanged meander thin-box instrument unconditionally at 1x, 2x, 4x, and 8x; release reads only the frozen 4x-to-8x convergence and NEC2-to-8x agreement gates. The paired-state scientific object, every threshold, and the thin-box formula remain unchanged, and no r3 or candidate run is authorized. |
| 2026-08-26 | Freeze the bounded semifinal 5.8 GHz anchor r3 ladder extension before any r3 solve | R2 correctly remained unreleased because its 4x minimum was outside the target band, making the frozen band-valid movement undefined while the 1x--8x sequence was still moving. R3 makes no geometry, scientific-object, threshold, score, mesh-baseline, budget, or parent-rule change: it reuses the exact r2 geometry SHA-256 and unconditionally extends only the refinement multipliers to 1x/2x/4x/8x/16x/32x. Prospectively, the last-pair movement uses full-sweep internal minima so an approaching out-of-band minimum is defined; target-band depth validity remains required only for NEC2 and 32x. Release reads only 16x-to-32x convergence and NEC2-to-32x agreement. This is a one-time bounded terminal study with no r4 and authorizes no candidate or exploration run. |
| 2026-08-27 | Pre-register the bounded 5.8 GHz resolved-rod renderer qualification and amend G5 sequencing | Qualify exactly one finite square-rod conductor plus matching finite-area port representation on the byte-identical r2 geometry with a fixed 1x/2x/4x/8x exterior-near-field ladder, constant CFL-proxy physical window, unchanged 5.8 GHz gates, and no result-driven retry. Rod does not replace the frozen thin-line mapping unless separately preregistered 5.8 and 2.45 GHz rod anchors both release. NEC2 remains the preregistered hypothesis-generation and ranking oracle. The rod anchors gate only openEMS candidate cross-check execution and the final cross-solver verdict. NEC2-only baseline, Random, ES-cold, and ES-warm runs may proceed under their existing preregistration, but openEMS results must never influence ranking, parent selection, or top-candidate selection. Until both 2.45 and 5.8 GHz rod anchors are released, the final verdict ceiling remains `insufficient_evidence`. This sequencing amendment preserves three invariants: the manual-baseline archive commit precedes every ES-warm evaluation, the unique warm-parent rule is unchanged, and every baseline/Random/ES implementation, test, and preregistration gate passes before numerical execution. |
| 2026-08-27 | Supersede v3.4 G5 and tests 8/9 for the paired NEC2 execution layer and timing preflight | NEC2 remains the preregistered hypothesis-generation and ranking oracle. The rod anchors gate only openEMS candidate cross-check execution and the final cross-solver verdict. NEC2-only baseline, Random, ES-cold, and ES-warm runs may proceed under their existing preregistration, but openEMS results must never influence ranking, parent selection, or top-candidate selection. Until both 2.45 and 5.8 GHz rod anchors are released, the final verdict ceiling remains `insufficient_evidence`. This supersedes only v3.4 section 15 G5 and section 10 tests 8/9: paired NEC2 exploration and openEMS release are decoupled; for this cycle `openems_cross_check_authorized` is `Literal[False]`, the verdict ceiling is constant, any release requires a later preregistration, and the archived rod-r1 failure is not rewritten. |
| 2026-08-27 | Freeze the 864-sweep discrete manual baseline and unique warm parent before execution | Execute exactly the v3.4 section 8 grid as 36 hardware rows times 12 A plus 12 B cached single-state lambda/20 NEC2 sweeps, then assemble all 12-by-12 pairs without another solve. Freeze the warm parent by valid-pair eligibility, base score descending, then hardware hash, hardware-grid index, and pair-grid index ascending. The coarse length tables are preregistered as structurally unlikely to land both narrow target bands and may yield zero valid pairs; no grid row, threshold, or second manual arm may be added. The baseline is isolated from agent candidate pools and this task starts no Random or ES arm. |
| 2026-08-29 | Freeze the nine-run paired-agent matrix and bind the unique warm parent before any agent evaluation | Run exactly Random, ES-cold, and ES-warm at seeds 101/202/303 with the archived preflight-capped budget 300, lambda/20 NEC2, strict subprocess-only execution, and no openEMS authorization. The unique warm parent is the diagnostic manual pair `e9f13ba6ede326e3adc4a48ba0a7658c0ca712434550ed98bffab681d262b321` committed in `906835eceeae2e48a652e2b7fa891fd3e8461440`; all three warm runs bind that commit and the byte-addressed parent while leaving the archived nullable placeholder untouched. Runs execute in the frozen Random, cold, warm order with deterministic resume; solver or evidence-integrity failure stops the matrix without configuration retry, while a frozen proposal-limit terminal result is reported and does not erase later independent arms. Candidate freezing remains NEC2-only and follows v3.4 section 11 after all nine runs; no openEMS, anchor, score, threshold, budget, proposer constant, or manual evidence byte changes. |
| 2026-08-29 | Pre-register one terminal 5.8 GHz rod-r2 port-observable harness repair | Rod r1 ran 23,716 openEMS steps and exited zero, but `port_ut_1` was absent, so it produced no usable port time series, S11 curve, or scientific verdict; a path mismatch is less likely but not excluded. This prospective entry supersedes exactly once only the r1 procedural clauses that missing port data or any execution failure terminates without retry, that there is one attempt and no rod r2, and that no result-driven retry is permitted, solely to run a frozen 10-step legacy-versus-official `AddLumpedPort` A/B harness diagnostic and, only if it passes, the unchanged rod 1x/2x/4x/8x ladder. Geometry, conductor, excitation, mesh, sweep, thresholds, four scientific verdicts, candidate identities, failed 4.674341% effect gate, score, budget, parent rule, and G5 ceiling remain unchanged. `repair_not_confirmed`, any r2 execution failure, or any non-released verdict is terminal; there is no rod r3. A released result authorizes only a future separately preregistered 2.45 GHz compatibility anchor and no present candidate or exploration run. |
| 2026-08-30 | Pre-register robust-hunt R2 as a post-freeze bounded parent-return ES gate-reproduction appendix | Restarts return to the frozen R2 parent through a new isolated proposer while the base config, existing ES, global box, score, thresholds, and ES constants remain byte-frozen. Endpoints are mutually exclusive by pass count, incomplete execution has no scientific endpoint, and zero of five means only that no crossing was observed under the frozen bounded study. The verdict ceiling remains `insufficient_evidence`; this does not modify `bdfb9c1` or authorize openEMS. |
| 2026-08-30 | Pre-register a feasibility-preserving, balanced-turn study after the terminal R2 appendix | R2's accepted search measure was overwhelmingly turn 3 and all 6,313 rejections were short-segment failures, so this independent study prospectively separates proposal representation from bounded NEC2 search. It preserves the physical integer support, section-4.3 geometry, 21-point audit, score, frequency grids, gate, and solver, while an exact conditional map removes analytically illegal short segments and fixed accepted quotas expose turns 3/4/5/6 equally. A solver-free common-random-number ablation and support-equivalence commit must precede a ten-run Random-versus-cold-ES matrix; every turn and seed is reported, no online reallocation is allowed, and openEMS remains unauthorized with verdict ceiling `insufficient_evidence`. |
| 2026-08-30 | Supersede the unexecuted stratified v1 with an exact-support v2 after its support gate failed | Deterministic terminal and height witnesses proved that v1's exact nominal Fraction clauses are not identical to the frozen binary64 audit's one-picometre tolerance sliver, so v1 remains unexecuted with `mapping_support_equivalence_not_established`. V2 uses a new study ID and applies the same exact nominal support to both legacy/conditional Stage-A representations and both Stage-B agents; it does not pretend to preserve audit-tolerance-only tuples. Short solver-free unit prefixes had tested stream mechanics, but no complete Stage-A cell, evidence artifact, endpoint, solver result, score, or candidate existed, and no prefix outcome changes v2. Seeds, streams, draw counts, map, quotas, budget, score, gates, endpoints, no-openEMS ceiling, and all frozen bytes remain unchanged; only study/version/run identities and the prospective exact-support definition are new. |
| 2026-08-31 | Pre-register the B-parent A-only conditional completion study before any new solver call | The completed exact-support Stage-B evidence contains exactly two byte-addressed records whose frozen state B independently passes its search-validity gate. This bounded prospective study freezes each complete hardware identity and B control, searches only the exact legal A support with Random and one cold restarted ES across five seeds, and reproduces the B geometry and NEC2 curve on every accepted record. A solver-free exhaustive support certificate precedes the fixed 20-run matrix; source blobs, inherited science code, random-stream consumption, endpoints, failure branches, and the `insufficient_evidence` ceiling are all committed before execution. No source-A value enters selection or initialization, no openEMS call or confirmation claim is authorized, and any terminal certificate, mapping, solver, or B-reproduction failure ends this v1 study without a scientific retry. |
| 2026-09-01 | Pre-register one bounded A-span support causal probe after the B-completion endpoint | Freeze one ES H1 source record per B-parent and seed, then hold hardware, state-A wire length, frozen state B, 40 mm physical box, geometry equation, score, frequency table, and lambda/20 NEC2 fixed while applying only additive state-A span-control doses of 0, +50,000, and +100,000 ppm. The diagnostic-only support extension does not change `box_size_um`, the old `StateControl`, the 702/0 evidence, or the candidate pool. A 630-geometry trajectory certificate, two B replays, and ten fresh zero-dose curve replays gate the twenty positive-dose calls; the complete run is capped at 32 real subprocess solves. Its four finite-set endpoints and `insufficient_evidence` ceiling are frozen before implementation, and no openEMS, retry study, or submission-claim update is authorized by this entry. |
| 2026-09-01 | Correct only the A-span probe''s Windows text-checkout identity gate before numerical execution | The first committed preflight inspection constructed no adapter and made no solver call, but correctly stopped because `core.autocrlf=true` made three clean Python worktree files CRLF while their frozen Git blobs remained LF. Source, implementation, and execution blob IDs were identical and Git''s path-aware filtered worktree hashes matched them exactly. Prospectively, only the seven frozen text-code paths use that Git filter-aware identity; all archived evidence and preregistration bytes remain raw-SHA checked. Cohort, doses, geometry, box, solver, frequencies, score, thresholds, 32-call order, endpoints, no-retry rule, and claim ceiling are unchanged. |
